knitr::opts_chunk$set(echo = TRUE)
install.packages("tinytex")
install.packages("rmarkdonw")
install.packages("tinytex")
```{r}
getwd()
knitr::opts_chunk$set(echo = TRUE)
# Check if the package is not installed (! operator), then install and load it
package_list <- c("dplyr", "readr", "tidyverse", "tidyr")
for (pckg in package_list) {
if (!requireNamespace(pckg, quietly = TRUE)) {
install.packages(cpkg, dependencies = TRUE)
}
library(pckg, character.only = TRUE)
}
getwd()
# Read in body_condition.csv
tortoise_body_condition <- readr::read_csv(
"Sex_ratio_bias_triggers_demographic_suicide-main/R/input/body_condition.csv"
)
# read in clutch_size.csv
clutch_size <- readr::read_csv(
"Sex_ratio_bias_triggers_demographic_suicide-main/R/input/clutch_size.csv"
)
knitr::opts_chunk$set(echo = TRUE)
# Check if the package is not installed (! operator), then install and load it
package_list <- c("dplyr", "readr", "tidyverse", "tidyr")
for (pckg in package_list) {
if (!requireNamespace(pckg, quietly = TRUE)) {
install.packages(pckg, dependencies = TRUE)
}
library(pckg, character.only = TRUE)
}
getwd()
str(clutch_size)
class(tortoise_body_condition)
names(tortoise_body_condition)
colnames(tortoise_body_condition)
names(tortoise_body_condition)
head(tortoise_body_condition)
View(tortoise_body_condition)
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column
names()
length(which(tortoise_body_condition$ind == 1))
tortoise_body_condition %>%
group_by((ind))%>%
summarise(numberTort = n())
tortoise_body_condition %>%
group_by((ind))%>%
summarise(numberTort = n())
#fixing 1, 2, 3, and 4 (removing unwanted columns)
#look at what we have
names(tortoise_body_condition) #command enter / ctrl+ enter
#change it
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column
names()
#did it do what we wanted?
#fix 5 -- grouping by individual
#look at our whole dataframe
View(tortoise_body_condition)
#how many rows belong to individual 1?
length(which(tortoise_body_condition$ind == 1)) # which function tests the logical test, length sees how many statements equal true
#how many rows belong to each individual?
tortoise_body_condition %>%
group_by((ind))%>%
summarise(numberTort = n()) # count lenth of each row of the groups
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column
names()
#fix 6 -- sorting by year_recode
#not currently ordered:
#did this fix it?
#fix 7 -- rename columns
#check current state. what do the abbreviations mean? --> have to get from paper
#did this fix it?
#fix 8 -- change order of columns
#what is the current order?
#did this fix it?
#fix 9 -- recode 'locality' column values
#what are all options of loc?
#did this fix it?
#fix 10 -- recode 'season' column values
#what are all options of season?
#did this fix it?
#save cleaned df to a new object
tortoise_body_condition_cleaned <- tortoise_body_condition
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column
group_by(ind) %>%
head()
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column # fixed problem 1-4
group_by(ind) %>% # fixed problem 5
View()
head(tortoise_body_condition)
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column (-c) # fixed problem 1-4
group_by(ind) %>% # fixed problem 5
arrange(year_recode, .by_group = TRUE) %>% # fix problem 6 -- will put code in ascending order
head()
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column (-c) # fixed problem 1-4
group_by(ind) %>% # fixed problem 5
arrange(year_recode, .by_group = TRUE) %>% # fix problem 6 -- will put code in ascending order
ungroup() %>%
rename(
straight_carapace_length_mm = SCL,
body_mass_grams = BM,
body_condition_index = BCI,
individual = ind,
locality = loc
) %>%
names()
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column (-c) # fixed problem 1-4
group_by(ind) %>% # fixed problem 5
arrange(year_recode, .by_group = TRUE) %>% # fix problem 6 -- will put code in ascending order
ungroup() %>%
rename(
straight_carapace_length_mm = SCL,
body_mass_grams = BM,
body_condition_index = BCI,
individual = ind,
locality = loc
) %>%
relocate(
individual,
year,
year_recode,
season,
locality,
sex,
body_mass_grams,
body_condition_index,
straight_carapace_length_mm
) %>%
names()
tortoise_body_condition %>% #pipe |>
select(-c(...1,res,log_BCI,loc_sex_cohort)) %>% #anti collect to remove column (-c) # fixed problem 1-4
group_by(ind) %>% # fixed problem 5
arrange(year_recode, .by_group = TRUE) %>% # fix problem 6 -- will put code in ascending order
ungroup() %>%
rename(
straight_carapace_length_mm = SCL,
body_mass_grams = BM,
body_condition_index = BCI,
individual = ind,
locality = loc
) %>%
relocate(
individual,
year,
year_recode,
season,
locality,
sex,
body_mass_grams,
body_condition_index,
straight_carapace_length_mm
) %>%
names()
